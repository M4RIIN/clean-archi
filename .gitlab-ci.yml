stages:
  - trigger-modules

trigger module 1/4:
  stage: trigger-modules
  trigger:
    include: consoleApp/console-app/gitlab-ci.yml
  only:
    changes:
      - consoleApp/console-app/*

trigger module 2/4:
  stage: trigger-modules
  trigger:
    include: consoleApp/manual-config/gitlab-ci.yml
  only:
    changes:
      - consoleApp/manual-config/*

trigger module 3/4:
  stage: trigger-modules
  trigger:
    include: core/domain/gitlab-ci.yml
  only:
    changes:
      - core/domain/*

trigger module 4/4:
  stage: trigger-modules
  trigger:
    include: database/gitlab-ci.yml
  only:
    changes:
      - database/*